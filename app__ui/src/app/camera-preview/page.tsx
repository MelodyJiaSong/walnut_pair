'use client';

// Camera Preview Page

import CameraPreviewContainer from '../../pages/cameraPreview/cameraPreview__container';

export default function CameraPreviewPage() {
  return <CameraPreviewContainer />;
}

